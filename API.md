# Menu API Documentation

REST-like API для системы бизнес-ланчей ресторана (ТРВП-008).  
Позволяет управлять вариантами меню, блюдами в меню, каталогом блюд и типами блюд.

- Формат: JSON
- Базовый URL: `/api`

---

## Сущности

### DishType
Тип блюда (фиксированный набор: салат, первое, второе, напиток, десерт).

Поля:
- `id` — идентификатор типа
- `name` — название типа
- `orderIndex` — порядок подачи

### Dish
Блюдо из каталога.

Поля:
- `id` — идентификатор блюда
- `name` — название блюда
- `type` — объект типа блюда (`DishType`)

### Menu
Вариант меню на день недели.

Поля:
- `id` — идентификатор меню
- `dayOfWeek` — день недели (1..7)
- `variantNum` — номер варианта в этот день (1..N)
- `dishes` — массив блюд (`Dish[]`)

---

## Меню

### Получить все меню с блюдами
`GET /api/menus`

Возвращаемый код: `200`

#### Пример ответа
```json
[
  {
    "id": 1,
    "dayOfWeek": 1,
    "variantNum": 1,
    "dishes": [
      {
        "id": 5,
        "name": "Оливье",
        "type": { "id": 1, "name": "Салат", "orderIndex": 1 }
      },
      {
        "id": 8,
        "name": "Борщ",
        "type": { "id": 2, "name": "Первое", "orderIndex": 2 }
      }
    ]
  }
]
```

### Получить блюда конкретного меню
`GET /api/menus/:menuId/dishes`

#### Параметры маршрута:
- `menuId` — идентификатор меню.

Возвращаемый код: `200`

#### Пример ответа

```json
[
  {
    "id": 12,
    "name": "Борщ",
    "type": { "id": 2, "name": "Первое", "orderIndex": 2 }
  }
]
```

### Создать пустое меню
`POST /api/menus`

Возвращаемый код: `201`

#### Параметры маршрута:
- `menuId` — идентификатор меню.

#### Пример запроса
```JSON
{
"dayOfWeek": 1,
"variantNum": 2
}
```


#### Пример ответа
```JSON
{
"id": 5,
"dayOfWeek": 1,
"variantNum": 2
}
```

### Изменить меню
`PATCH /api/menus/:menuId`

Возвращаемый код: `200`

#### Параметры маршрута:
- `menuId` — идентификатор меню.

#### Пример запроса
```JSON
{
"dayOfWeek": 1,
"variantNum": 1
}
```

#### Пример ответа
```JSON
{
  "id": 1,
  "dayOfWeek": 1,
  "variantNum": 1,
  "dishes": [
    {
      "id": 5,
      "name": "Оливье",
      "type": { "id": 1, "name": "Салат", "orderIndex": 1 }
    },
    {
      "id": 8,
      "name": "Борщ",
      "type": { "id": 2, "name": "Первое", "orderIndex": 2 }
    }
  ]
}
```

### Удалить меню
`DELETE /api/menus/:menuId`

Удаляет меню и все связи блюд с этим меню.

Возвращаемый код: `204`

#### Параметры маршрута:
- `menuId` — идентификатор меню.

## Блюда в меню
В рамках одного меню не должно быть двух блюд одного типа. При попытке нарушить правило будет получена ошибка.

### Добавить блюдо в меню
`POST /api/menus/:menuId/dishes`

Возвращаемый код: `201`

#### Параметры маршрута:
- `menuId` — идентификатор меню.

#### Пример запроса
```JSON
{ "dishId": 12 }
```

#### Пример ответа

```JSON
{
  "id": 12,
  "name": "Борщ",
  "type": { "id": 2, "name": "Первое", "orderIndex": 2 }
}
```

### Переместить блюдо в другое меню
`PATCH /api/menus/:menuId/dishes/:dishId`

Возвращаемый код: `200`

#### Параметры маршрута:
- `menuId` — исходное меню.
- `dishId` — идентификатор блюда.

#### Пример запроса
```JSON
{
"newMenuId": 5
}
```
#### Пример ответа
Возвращает меню, в которое было перемещено блюдо
```JSON
{
"id": 5,
"dayOfWeek": 5,
"variantNum": 1,
"dishes": [
{
"id": 5,
"name": "Оливье",
"type": { "id": 1, "name": "Салат", "orderIndex": 1 }
},
{
"id": 8,
"name": "Борщ",
"type": { "id": 2, "name": "Первое", "orderIndex": 2 }
}
]
}
```

### Удалить блюдо из меню
`DELETE /api/menus/:menuId/dishes/:dishId`

Возвращаемый код: `204`

#### Параметры маршрута:
- `menuId` — идентификатор меню.
- `dishId` — идентификатор блюда.

## Каталог блюд
#### Получить список всех блюд
`GET /api/dishes`

Возвращаемый код: `200`

#### Пример запроса
````JSON
[
{
"id": 5,
"name": "Оливье",
"type": { "id": 1, "name": "Салат", "orderIndex": 1 }
},
{
"id": 8,
"name": "Борщ",
"type": { "id": 2, "name": "Первое", "orderIndex": 2 }
}
]
````

### Добавить новое блюдо в каталог
`POST /api/dishes`

Возвращаемый код: `201`

#### Пример запроса
````JSON
{
"name": "Оливье",
"typeId": 1
}
````

#### Пример ответа
````JSON
{
  "id": 5,
  "name": "Оливье",
  "type": { "id": 1, "name": "Салат", "orderIndex": 1 }
}
````

### Удалить блюдо из каталога
`DELETE /api/dishes/:dishId`

Возвращаемый код: `204`

Блюдо также будет удалено из всех меню, где оно использовалось.

#### Параметры маршрута:
- `dishId` — идентификатор блюда.

## Типы блюд
### Получить список типов блюд
`GET /api/dishTypes`

Возвращаемый код: `200`

#### Ответ
```JSON
[
{ "id": 1, "name": "Салат", "orderIndex": 1 },
{ "id": 2, "name": "Первое", "orderIndex": 2 },
{ "id": 3, "name": "Второе", "orderIndex": 3 },
{ "id": 4, "name": "Напиток", "orderIndex": 4 },
{ "id": 5, "name": "Десерт", "orderIndex": 5 }
]
```

#### Коды ошибок

- `400 Bad Request` — неверный JSON / неверные значения полей.
- `404 Not Found` — ресурс не найден.
- `409 Conflict` — нарушение бизнес-правил (например, дубликат типа блюда в одном меню).

## Бизнес-правила
- Типы блюд фиксированы.
- В одном меню не может быть двух блюд одного типа.
- Блюда в меню должны сортироваться по type.orderIndex.